# Copie este arquivo para .env e preencha com os valores combinados pela equipe.
# O .env NÃO vai para o repositório — cada pessoa mantém o seu.

# Banco de dados
DB_HOST=localhost
DB_PORT=5432
DB_NAME=caderno_fazenda
DB_USER=
DB_PASSWORD=

# Ambiente: desenvolvimento | producao
APP_ENV=desenvolvimento
